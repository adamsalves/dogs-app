import React from "react";
import UserHeader from "./UserHeader";

const User = () => {
  return (
    <section className="container">
      <UserHeader />
    </section>
  );
};

export default User;
